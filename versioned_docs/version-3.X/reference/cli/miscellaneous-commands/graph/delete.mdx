---
sidebar_label: delete
pagination_label: graph delete
---

# `graph delete` Command

The `graph delete` command deletes an infrastructure graph.

## Usage

```bash
graph delete <graph_name>
```

## Example

```bash
> graph delete resoto_backup
# highlight-start
Graph resoto_backup deleted.
# highlight-end
```
